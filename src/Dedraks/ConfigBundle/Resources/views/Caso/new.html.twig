{# empty Twig template #}
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}



<script>
    
    {#//var c="{{create}}";#}

    {% if id is defined %}
        var url = Routing.generate('configuracoes_caso_tipo_edit', {'id':{{id}}});
    {% else %}
        var url = Routing.generate('configuracoes_caso_tipo_new');
    {% endif %}
    
    {# var url = (c==='sim') ? Routing.generate('configuracoes_caso_tipo_new') : Routing.generate('configuracoes_caso_tipo_edit', {'id':{{id}}}); #}
    
    //alert(url);
    
    
    $('#newTipoCasoForm').ajaxForm({
        url: url,
        type: 'post',
        success: function(responseText) {
            $("#dataArea").html(responseText);
            window.configPopup.close();
        }
    });
</script>